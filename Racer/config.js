//
// Engine configuration
//

//Game fonts
const main_game_font = 'tronregular'

const secondary_game_font = 'tronsecondary';

const tron_blue = '#18CAE6'

const CONFIG_lapsToWin = 3;

const initial_pickup_counter = 2;

const player_name_max_length = '4';

const player1Name = 'P1';

const player2Name = 'P2';

const track_friction = 0.05; // Smaller number means more slippery surface

const player_mass = 20; // Larger number means it takes longer to accelerate to top speed

const player_move_speed = 8; // Bigger number = faster!

const player_top_speed = 10;

const player_rotate_speed = 0.3; // Bigger number = faster!

const pickup_sprite_scale = 0.75; // Bigger number = larger sprite!

const pickup_time_delay = 5; // Delay (in seconds) between coins appearing


const points_on_hit = 100; // depricated
const player_sprite_scale = 2.5; // depricated
const integrator_step = 1.0; // depricated
const projectile_speed = 0.1; // depricated
const frictionCoeff = 0.025; // depricated
const bulletRechargeTime = 1; // depricated
const bullet_scale = 1.5; // depricated
const bullet_lifespan = 200; // depricated
const fps = 60; // depricated
